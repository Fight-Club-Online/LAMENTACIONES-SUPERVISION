global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: 'supervision-service'

    metrics_path: '/actuator/prometheus'

    scheme: https

    basic_auth:
      username: 'admin'
      password: 'lamentaciones2026'

    static_configs:
      - targets:
          - 'supervision-lamentaciones-d8d3a5bpd6gvcva8.canadacentral-01.azurewebsites.net'